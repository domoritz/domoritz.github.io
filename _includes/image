{% capture imagePath %}{% if include.name %}{{ '/images/' | relative_url }}{{include.name}}{% else %}{{ include.url }}{% endif %}{% endcapture %}
{% if include.caption %}
<figure>
    <a href="{{ imagePath }}"><img src="{{ imagePath }}" alt="{{ include.caption }}" {% if include.width %} width="{{ include.width }}" {% endif %}></a>
    <figcaption><span markdown="1">{{ include.caption }}</span></figcaption>
</figure>
{% else %}
<a href="{{ imagePath }}"><img src="{{ imagePath }}" {% if include.alt %} alt="{{ include.alt }}" {% endif %} {% if include.width %} width="{{ include.width }}" {% endif %}></a>
{% endif %}
